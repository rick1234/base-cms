# Default Template

This folder is reserved for Blade partials and layout overrides for the reusable `default` website template.

Shared base markup lives in `resources/views/frontend`. Forked websites can add customer-specific overrides here without scattering project code through the reusable base.
